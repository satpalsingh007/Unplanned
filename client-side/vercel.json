{
    "version": 2,
    "builds": [
        {
            "src": "index.html",
            "use": "@vercel/static",
            "config": {
                "distDir": "dist"
            }
        }
    ],
    "routes": [{ "src": "/(.*)", "dest": "/" }]
}
